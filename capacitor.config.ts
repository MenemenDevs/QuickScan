
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quickscan.id',
  appName: 'QuickScan ID',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
